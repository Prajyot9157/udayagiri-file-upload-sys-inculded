export interface AppMaterial {
  id: string;
  category: string;
  subject: string;
  linkType: string;
  topic: string;
  code: string;
  size?: string;
  meetingId?: string;
  passcode?: string;
  timestamp?: number;
}
